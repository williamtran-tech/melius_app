// BMI calculation formula: weight (kg) / [height (m)]2

// TDEE calculation formula: 10 * weight (kg) + 6.25 * height (cm) - 5 * age (y) + s (kcal / day)
import { User } from "./../orm/models/user.model";
import { Account } from "./../orm/models/account.model";

export default class HealthService {
  public async calculateBMI(kidData: any) {
    try {
    } catch (err) {
      throw err;
    }
  }

  public calculateTDEE(kidData: any) {
    try {
    } catch (err) {
      throw err;
    }
  }
}
